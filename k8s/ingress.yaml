apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: qianxue-test-lifeline-ingress
  namespace: qianxue-test-lifeline-ns
  labels:
    app: qianxue-test-lifeline
  annotations:
    kubernetes.io/ingress.class: "nginx"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/gzip-types: "text/plain,text/css,application/json,application/javascript,text/xml,application/xml,application/xml+rss,text/javascript"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - test-lifeline.qianxue.online
    secretName: qianxue-test-lifeline-tls-secret
  rules:
  - host: test-lifeline.qianxue.online
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: qianxue-test-lifeline-service
            port:
              number: 80
